<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>RPS PvE</title>
  </head>
  <body>
    <header>
      <h2>This match:</h2>
    <h1><%= @game.player_1.name %> V Computer</h1>
    </header>
    <main>
      <br>
      <br>
      <h2> Choose your moves (no peeking!) </h2>
      <br>
      <h3>You may each secretly enter "roc"(k), "pap"(er) or "sci"(ssors)</h3>
      <br>

      <form action="/move-submit" method="post" class="row g-3">
        <div class="col-md-6">
          <label for="player_1_choice" class="form-label"><%= @game.player_1.name %>'s Move:</label>
          <input type="password" class="form-control" id="player_1_choice">
        </div>
        <div class="col-md-6">
          <label for="player_2_choice" class="form-label"><%= @game.player_2.name %>'s Move:</label>
          <input type="password" class="form-control" id="player_2_choice">
        </div>
        <div class="col-12">
          <button type="submit" class="btn btn-primary" name="player_choices">Go!</button>
        </div>
      </form>
    </main>
  </body>
</html>
