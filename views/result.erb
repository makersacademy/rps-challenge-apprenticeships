<div style="display: flex; justify-content: center; align-items: center; flex-direction: column; font-family: monospace;">
	<h1>Rock Paper Scissors!</h1>
	<h1>Tada!</h1>
	<h2>Guess what...</h2>
	<h3>
	<%# Don't use '=' after '%'
			'=' will print codes after
			without '=' code will only be evaluated  %>
	<%= $player_name %> chose: <%= $player_option %> vs. Computer chose: <%= $computer_option %>
	</h3>
	<h3>
	<% if $tie == true %>
		It is a tie!
		<% elsif $win == true %>
		<%= $player_name %> won!
		<% else %>
		<%= $player_name %> lost!
	<% end %>
	</h3>

	<form action='/' method='get'>
		<button type='submit' name='replay' value='play_again' style='font-family: monospace'>Play Again</button>
	</form>
</div>